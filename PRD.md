# 家庭记账 App — 产品需求文档（PRD）

> 版本：v0.2（需求已确认，v1 网页端开发中）
> 状态：开发中
> 适用范围：第一版（网页端）开发依据；小程序、iOS 为后续版本，架构需预留。

---

## 1. 产品概述

### 1.1 产品定位
一款面向**家庭**的记账与财务管理工具。家庭成员在同一平台记账、管钱、设预算、看报表，支持公共账本（全家共用）与个人账本（个人私密）两种记账模式。

### 1.2 目标用户
家庭全体成员，覆盖日常消费记账、家庭收支管理、负债管理（信用卡 / 房贷 / 车贷）、储蓄与理财记录等场景。

### 1.3 整体目标与版本规划
产品整体目标覆盖**浏览器网页、安卓 App、iOS App、微信小程序**四个端。

| 版本 | 客户端范围 | 说明 |
|---|---|---|
| **v1（本次）** | **网页端** | 唯一入口，配独立云端后端 |
| v2 | 微信小程序 | 复用后端 API |
| v3 | iOS App | 复用后端 API |
| 后续 | 安卓 App 等 | 复用后端 API |

**架构要求**：后端做成**独立 API 服务**（登录、账本数据存储、数据同步、统一接口层），网页端、小程序、iOS 均通过同一套接口调用，新增端只增加客户端，不重构后端。

---

## 2. 核心概念定义

- **用户**：使用本产品的自然人，通过微信授权或手机号验证码注册登录。
- **家庭**：一组用户的集合，成员可共享公共账本。
- **账本**：记账的数据容器，分「公共账本」与「个人账本」两类；一个用户可拥有多个账本。
- **账单**：一笔收支 / 转账记录，归属于某个账本。
- **账户**：钱的载体，分为三类——**资金账户**（现金 / 银行卡 / 微信 / 支付宝 / 京东钱包 / 抖音钱包等金融平台账户）、**信贷账户**（信用卡 / 花呗 / 京东白条 / 美团月付 / 房贷 / 车贷 / 装修贷等，记录欠款额）、**储值账户**（会员卡 / 公交卡 / 加油卡 / 购物卡等）。记账必选账户，支持多账户组合付款。
- **分类**：账单的归类维度（支出 / 收入各一套），由账本创建者管理。
- **标签**：分类之下的细分维度（如分类"餐饮"下可设"聚餐 / 外卖"），由账本创建者管理，记账时从库中选择。
- **预算**：按分类设定的支出额度，周期可自定义，超支有提醒与标记。

---

## 3. 功能需求

### 3.1 登录与家庭
- **登录方式（v1）**：手机号验证码登录为主；微信授权登录待资质（已备案域名 + 企业主体）具备后接入，接口预留开关。
- **首次引导**：登录后若无家庭 → 弹出「创建家庭 / 加入家庭」弹框（输入家庭名称创建，或输入邀请码加入），可跳过、随时可从右上角用户菜单再次进入。
- **组建家庭**：用户可创建家庭并成为创建者。
- **邀请成员**：创建者生成邀请链接 / 邀请码，或输入对方手机号邀请加入。
- **查看成员**：所有成员可查看家庭成员列表（头像、昵称、角色、加入时间）。
- **家庭入口**：左侧导航**不设"家庭"菜单**，家庭入口统一放在**右上角用户下拉菜单**（有家庭 → 进入家庭管理页；无家庭 → 弹出创建 / 加入弹框）。

### 3.2 账本
- **账本类型**：公共账本（全家共用，所有成员可见所有账单）与个人账本（个人私密记账）。
- **多账本**：一个用户可拥有多个账本，类型可混用。
- **新建 / 删除账本**：所有用户可新建账本（选类型 → 名称 → 图标 → 主题外观）；删除需二次确认并提示影响。
- **创建者权限（账本管理）**：
  - 成员管理：邀请成员、移除成员；
  - 分类管理：增删改、启停用；
  - 标签管理：维护标签库；
  - 主题外观管理：账本外观设置。
- **成员记账权限**：所有成员均可记账、改账、删账。
- **操作留痕**：账单的修改、删除必须记录操作日志（操作人、时间、变更内容），账单详情可查看；留痕以**时间线卡片**展示：操作类型彩色标签（创建绿 / 修改橙 / 删除红）+ 可读变更摘要（类型 · 金额 · 账户 · 备注）+ 操作人 + 时间。

### 3.3 记账
- **账单类型**：支出（默认）、收入、转账。借入 / 借出**暂不做**（后续版本）。
- **记账流程（四步）**：
  1. **账本与类型**：选账本（默认当前账本）；类型默认**支出**，可一键切换为收入 / 转账。
  2. **金额与分类**：大号金额输入框（支持小数、数字键盘布局）；分类图标网格（按支出 / 收入分别展示，可搜索）。转账步骤不选分类。
  3. **账户**：
     - 支出：付款账户，**支持多选并分配金额**（各账户分配金额合计必须等于总金额，否则不可提交）；
     - 收入：收款账户（支持多账户）；
     - 转账：转出账户 → 转入账户 → 金额，自动生成双边流水，总资产不变。
  4. **补充信息**：
     - **记账人**：默认当前登录成员；**公共账本**中可切换选择其他家庭成员（支持代记）；个人账本固定为本人，不显示切换；
     - **日期**：默认今天，可改，支持精确到时分；
     - **备注**：选填；
     - **标签**：多选，从账本标签库中选择（创建者维护标签库，成员只选不新增，避免标签泛滥）。
- **连续记账**：提交后保留上次账户 / 标签选择，支持"再记一笔"，方便批量记账。
- **账单列表**：按日期分组；每笔显示「记账人头像 + 分类图标 + 分类名 + 备注首句 + 标签 + 账户 + 金额 + 时间」；支持筛选（类型 / 分类 / 账户 / 成员 / 日期范围）与搜索。
- **转账展示**：转账为一种账单类型，统计中默认不计入收支。

### 3.4 账户资产
- **账户类型（三类）**：
  - **资金账户（asset）**：现金、微信、支付宝、银行卡，以及京东钱包、抖音钱包、零钱通、余额宝等金融平台账户（成员可维护余额）；
  - **信贷账户（credit）**：信用卡、花呗、京东白条、美团月付、抖音月付，以及房贷、车贷、装修贷等（记录欠款额，视觉上区别于正资产，支出增加欠款、还款减少欠款）；
  - **储值账户（stored_value）**：会员卡、公交卡、加油卡、购物卡等。
- **新增账户**：选类型 → 该类型下展示**推荐账户快捷标签**（点击即填充名称）→ 也可**自定义账户名称** → 填期初余额（信贷账户填欠款额）。
- **余额维护**：手动调整余额需填写"调整后余额 + 原因"，留痕记录。
- **资产总览**：总资产、总负债、净资产三张大卡（资金 + 储值计入总资产，信贷计入总负债），下方按账户类型分块展示。
- **账户详情**：当前余额 + 该账户收支 / 转账流水 + 余额调整记录。

### 3.5 预算
- **预算维度**：按分类设置预算（如"餐饮每月 2000"）。
- **周期**：支持自定义（按周 / 按月 / 自定义起止日期）。
- **超支提醒与标记**：超支分类标红 + 醒目提示；预算页面展示进度条（预算金额 / 已用 / 剩余）；首页入口展示进度靠前的分类。
- 推送通知**暂不做**，第一版以站内标记为主。

### 3.6 统计（第一版）
- **收支趋势**：折线 / 柱状图，可按日 / 周 / 月切换，可框选时间范围。
- **分类占比**：饼图 + 图例明细，展示各分类支出占比。
- 两个图表均带筛选器（账本、时间范围、只看支出 / 收入）。
- 其余统计（资产负债总表、预算执行报表、按成员统计等）**暂不做**，后续版本补充。

### 3.7 设置
- **账户信息**：查看头像 / 昵称 / 登录方式（只读，不做修改、解绑）。
- **成员查看**：查看家庭成员信息（可跳转家庭模块）。
- **账本管理**：列出所有账本，新建账本入口、删除账本（二次确认）。
- 改密码 / 解绑微信、提醒通知开关、数据导出 / 清空账本：**暂不做**。

---

## 4. 页面结构与交互设计

### 4.1 整体页面地图

```
登录 / 注册
  └── 首次引导（新用户：创建家庭 or 加入已有家庭，弹框形式）

主应用（登录后，左侧导航 + 顶部栏 + 内容区）
├── 首页（概览 Dashboard）
├── 账本
│   ├── 账本列表（我的账本：公共 / 个人）
│   ├── 新建账本
│   ├── 账本详情（账本内账单流水 + 筛选）
│   ├── 记账（新建 / 编辑账单，全站复用）
│   └── 账本管理（仅创建者：成员 / 分类 / 标签 / 外观）
├── 账户资产
│   ├── 资产总览（总资产 / 总负债 / 净资产）
│   ├── 账户列表（资金账户、信贷账户、储值账户分组）
│   ├── 新增账户（推荐账户 + 自定义）
│   └── 账户详情（余额 + 变动流水）
├── 预算
│   ├── 预算总览（按分类列出，进度条）
│   └── 新建 / 编辑预算（分类 + 金额 + 自定义周期）
├── 统计
│   ├── 收支趋势（折线 / 柱状）
│   └── 分类占比（饼图）
└── 设置
    ├── 账户信息（只读）
    ├── 成员查看（可并入家庭模块）
    └── 账本管理（新建 / 删除账本）

家庭模块（入口在右上角用户下拉菜单，非左侧导航）
├── 成员列表（创建者 / 成员标识）
└── 邀请成员（邀请链接 / 邀请码 / 输手机号）
```

### 4.2 主框架与导航
- **左侧导航**：首页 / 账本 / 账户 / 预算 / 统计 / 设置（**不含"家庭"**），当前项高亮。
- **顶部栏**：当前账本切换器（下拉，显示账本名 + 类型标识"公共 / 个人"）+ 全局「+ 记账」按钮 + 用户头像 / 昵称下拉（含**家庭**入口与退出登录）。
- **全局快捷入口**：右上角常驻「+」按钮，任何页面可进入记账流程。
- **核心交互锚点**：记账、预算、统计均**跟随当前账本**，切换账本后各页数据随之切换。

### 4.3 关键页面交互
- **登录 / 首次引导**：微信扫码与手机号验证码两个 tab；无家庭时弹框引导创建或输入邀请码加入（可跳过，随时从右上角用户菜单再次打开）；首次进入引导建账本（默认建议"家庭公共账本"）。
- **首页**：本月收支概览卡片、净资产卡片、预算执行进度（前 3 个分类，超支标红）、最近账单（5~10 笔）、收支趋势迷你图（近 30 天）。
- **账本详情**：顶部余额 / 统计小卡，下方账单流水（按日期分组，支出红、收入绿、转账蓝灰），支持筛选与搜索。
- **记账**：居中弹窗或独立页面，支持连续记账；详见 3.3 四步流程。
- **账户资产**：按资金 / 信贷 / 储值分组展示；信贷账户红 / 橙视觉区分；余额调整留痕。
- **预算**：进度条展示，超支标红 + 提示。
- **统计**：趋势图与占比图带筛选器。

### 4.4 贯穿全站的关键交互
1. **操作留痕**：修改 / 删除账单弹窗明示"该操作将记录在案"；账单详情展示操作记录（谁、何时、变更内容）。
2. **多账户组合付款**：金额分配输入框实时校验"分配合计 = 总金额"，不满足不能提交。
3. **转账**：一步完成，自动生成双边流水，避免手动记两笔。
4. **空状态设计**：账本、账户、账单、预算、家庭成员均有引导性空状态。
5. **权限可见性**：非创建者不显示"账本管理"入口；记账、改账、删账权限人人都有（留痕兜底）。

---

## 5. 非功能需求

- **多端可扩展**：后端为独立 API 服务，接口统一，为小程序 / iOS 预留接入能力。
- **数据同步**：多端、多成员数据实时 / 准实时同步（家庭公共账本）。
- **权限与安全**：登录态校验；操作留痕（审计日志）；数据按账本 / 家庭隔离。
- **易用性**：记账流程控制在 4 步以内，支持快速连续记账。
- **数据可恢复**：删除账本需二次确认并提示影响，避免误删。

---

## 6. 暂不做（后续版本）

- 微信小程序、iOS App（架构预留，v2 / v3 实施）
- 微信授权登录（第一版暂缓，待备案域名 + 企业主体资质具备后接入）
- 借入 / 借出
- 自动记账 / 账单导入
- 资产负债总表、预算执行报表、按成员统计等更多统计维度
- 改密码 / 解绑微信、提醒通知开关、数据导出 / 清空账本
- 复杂的成员权限体系（如成员分组）

---

## 7. 附录：已确认的关键决策

| 决策点 | 结论 |
|---|---|
| 第一版客户端 | 网页端 |
| 登录方式 | v1 手机号验证码；微信授权登录待资质（备案域名 + 企业主体）后接入 |
| 账本模型 | 多账本；公共 / 个人两类；公共账本全员可见所有账单 |
| 公共账本权限 | 创建者管理（成员 / 分类 / 标签 / 外观）；所有成员可记账；改删留痕 |
| 账户体系 | 三类账户：资金（asset）/ 信贷（credit）/ 储值（stored_value）；新增账户有推荐快捷选择 + 自定义入口；记账必选账户，支持多账户组合付款 |
| 操作留痕 | 改删写审计日志；账单详情以时间线卡片展示（操作类型标签 + 可读摘要 + 操作人 + 时间） |
| 预算 | 按分类；周期自定义；超支提醒 + 标记 |
| 统计（v1） | 收支趋势 + 分类占比 |
| 账单类型 | 支出（默认）/ 收入 / 转账 |
| 记账人 | 公共账本可切换代记，个人账本固定本人 |
| 标签 | 创建者维护标签库，成员只选不新增 |
| 设置（v1） | 账户信息只读查看、成员查看、新建 / 删除账本 |
| 家庭入口 | 左侧导航不设"家庭"，入口在右上角用户下拉；无家庭时以弹框创建 / 加入 |
